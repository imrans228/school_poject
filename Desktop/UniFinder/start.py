# start.py - Скрипт для запуска приложения на хостинге

from waitress import serve
from app import app
import os
import sys

# Render автоматически устанавливает переменную PORT.
PORT = int(os.environ.get('PORT', 5000))

print("-" * 50)
print(f"🚀 Запуск производственного сервера Waitress на порту {PORT}...")
print("-" * 50)

# Запускаем приложение Flask (app) на 0.0.0.0, чтобы оно было доступно снаружи
serve(app, host='0.0.0.0', port=PORT)
